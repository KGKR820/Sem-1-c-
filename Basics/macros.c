#include <stdio.h>
#define pi 3.14159265359 // here we are defining thr value of pi
#define area(r) (pi*r*r) // here we are defining a function area
int main(){
printf("%f",area(9));
return 0;
}