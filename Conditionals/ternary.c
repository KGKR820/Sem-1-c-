#include <stdio.h>
int main(){
int x;
printf("Enter the number : ");
scanf("%d",&x);
// (condition) ? do this if condn satifies : else do this;
(x%2==0) ? printf("even") : printf("odd");
// if and else 
    return 0;
}